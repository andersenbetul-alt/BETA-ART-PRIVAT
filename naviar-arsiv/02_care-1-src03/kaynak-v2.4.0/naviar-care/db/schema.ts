import { sql } from 'drizzle-orm';
import { sqliteTable, text, integer, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
export const slots=sqliteTable('slots',{id:text('id').primaryKey(),start:integer('start').notNull(),end:integer('end').notNull(),demo:integer('demo').notNull().default(1),active:integer('active').notNull().default(1)},t=>[uniqueIndex('slots_start').on(t.start).where(sql`${t.active} = 1`)]);
export const bookings=sqliteTable('bookings',{id:text('id').primaryKey(),slotId:text('slot_id').notNull().references(()=>slots.id),owner:text('owner').notNull(),idem:text('idem').notNull(),service:text('service').notNull(),name:text('name').notNull(),email:text('email').notNull(),locale:text('locale').notNull(),relationship:text('relationship').notNull(),status:text('status').notNull().default('confirmed'),demo:integer('demo').notNull(),created:integer('created').notNull(),total:integer('total'),offerDetails:text('offer_details'),offerVersion:integer('offer_version').notNull().default(0),payment:text('payment').notNull().default('not_requested'),stripeSession:text('stripe_session'),checkoutStarted:integer('checkout_started')},t=>[uniqueIndex('booking_idem').on(t.owner,t.idem),index('booking_owner').on(t.owner),index('booking_created').on(t.created),uniqueIndex('booking_slot_active').on(t.slotId).where(sql`${t.status} = 'confirmed'`)]);
export const messages=sqliteTable('messages',{id:text('id').primaryKey(),owner:text('owner').notNull(),idem:text('idem').notNull(),name:text('name').notNull(),email:text('email').notNull(),topic:text('topic').notNull(),body:text('body').notNull(),locale:text('locale').notNull(),demo:integer('demo').notNull(),created:integer('created').notNull(),status:text('status').notNull().default('new'),version:integer('version').notNull().default(0),updated:integer('updated')},t=>[uniqueIndex('message_idem').on(t.owner,t.idem)]);
export const outbox=sqliteTable('outbox',{id:text('id').primaryKey(),bookingId:text('booking_id').notNull(),kind:text('kind').notNull(),state:text('state').notNull().default('pending'),attempts:integer('attempts').notNull().default(0),providerId:text('provider_id'),created:integer('created').notNull(),lastAttempt:integer('last_attempt')});
export const webhookEvents=sqliteTable('webhook_events',{id:text('id').primaryKey(),created:integer('created').notNull()});
export const events=sqliteTable('events',{id:text('id').primaryKey(),session:text('session').notNull(),kind:text('kind').notNull(),section:text('section').notNull(),locale:text('locale').notNull(),day:text('day').notNull(),created:integer('created').notNull(),live:integer('live').notNull().default(0)},t=>[uniqueIndex('event_once').on(t.session,t.kind,t.section,t.day),index('event_date').on(t.created)]);
export const audit=sqliteTable('audit',{id:text('id').primaryKey(),actor:text('actor').notNull(),action:text('action').notNull(),target:text('target').notNull(),created:integer('created').notNull()});
export const rateLimits=sqliteTable('rate_limits',{key:text('key').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()});
